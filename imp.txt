created a tcp socket using socket()
with arguments =>
socket(AF_INET, SOCK_STREAM, 0);

if socket creation fails i.e if tcp socket = -1
then error is returned 

https://man.archlinux.org/man/sockaddr_in.3type

struct sockaddr_in {
    sa_family_t     sin_family;     /* AF_INET */
    in_port_t       sin_port;       /* Port number */
    struct in_addr  sin_addr;       /* IPv4 address */
};

sin_addr is a struct ..

struct in_addr {
    in_addr_t s_addr;
}; 

then binding part 
https://man.archlinux.org/man/bind.2

as the name suggests binding something ... 
bind() tells the kernel to attach this socket to this local address

addr = [ip, port]
until you call bind() ... socket is just a nameless endpoint 

what basically happening till now is 
we are creating a tcp socket and attach it to any local ipv4 addr, port 1337

(0-255.0-255.0-255.0-255, 1337) -> tcp_socket

then comes listen()

int listen(int sockfd, int backlog);
https://man.archlinux.org/man/listen.2

backlog is how many completed TCP connections the kernel is allowed to hold for you while you are not calling accept() yet.

SOMAXCONN -> upper bound enforced by the OS
it means .. i allow as many queued connections as the kernel allows

now accept()
https://man.archlinux.org/man/accept.2
accept() blocks until the kernel has a completed TCP connection, then returns a new socket file descriptor representing that single client connection.